<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ header.name }} - {{ header.title }}</title>
    <style>
        /* ATS-Friendly Resume Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            size: Letter;
            margin: 0.5in;
        }

        body {
            font-family: 'Arial', 'Helvetica', sans-serif;
            font-size: 11pt;
            line-height: 1.4;
            color: #000;
            max-width: 8.5in;
            margin: 0 auto;
            padding: 0;
            background: #fff;
            orphans: 2;
            widows: 2;
        }

        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 15px;
            border-bottom: 2px solid #000;
            padding-bottom: 8px;
        }

        .header h1 {
            font-size: 24pt;
            font-weight: bold;
            margin-bottom: 5px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .header h2 {
            font-size: 14pt;
            font-weight: normal;
            margin-bottom: 8px;
            color: #333;
        }

        .contact-info {
            font-size: 10pt;
            line-height: 1.6;
        }

        .contact-info a {
            color: #000;
            text-decoration: none;
        }

        /* Section Headings */
        .section {
            margin-bottom: 15px;
        }

        .section-title {
            font-size: 14pt;
            font-weight: bold;
            text-transform: uppercase;
            border-bottom: 1px solid #000;
            margin-bottom: 8px;
            padding-bottom: 2px;
            letter-spacing: 0.5px;
            page-break-after: auto;
            break-after: auto;
        }

        /* Summary Section */
        .summary-content {
            text-align: justify;
            margin-bottom: 0;
            orphans: 2;
            widows: 2;
        }

        .section.summary-section {
            margin-bottom: 12px;
            page-break-after: auto;
        }

        /* Experience Section */
        .section.experience-section {
            page-break-before: auto;
            break-before: auto;
            margin-bottom: 12px;
        }

        .experience-item {
            margin-bottom: 12px;
            margin-top: 0;
            padding-top: 0;
        }

        .experience-item:first-of-type {
            margin-top: 0 !important;
            padding-top: 0 !important;
        }

        .experience-header {
            margin-bottom: 5px;
        }

        .job-title {
            font-size: 12pt;
            font-weight: bold;
        }

        .company-name {
            font-size: 11pt;
            font-weight: bold;
        }

        .job-meta {
            font-size: 10pt;
            color: #333;
            margin-bottom: 5px;
        }

        .achievements {
            margin-left: 20px;
            margin-top: 5px;
        }

        .achievements li {
            margin-bottom: 5px;
        }

        .technologies {
            font-size: 10pt;
            margin-top: 5px;
            color: #333;
        }

        .technologies strong {
            font-weight: bold;
        }

        /* Education Section */
        .education-item {
            margin-bottom: 15px;
        }

        .degree-title {
            font-size: 12pt;
            font-weight: bold;
        }

        .institution-name {
            font-size: 11pt;
            font-weight: bold;
        }

        .education-meta {
            font-size: 10pt;
            color: #333;
            margin-bottom: 5px;
        }

        .honors-list, .coursework-list {
            margin-left: 20px;
            margin-top: 5px;
        }

        .honors-list li, .coursework-list li {
            margin-bottom: 3px;
        }

        /* Skills Section */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-top: 10px;
        }

        .skill-category {
            margin-bottom: 10px;
        }

        .skill-category-title {
            font-weight: bold;
            font-size: 11pt;
            margin-bottom: 5px;
        }

        .skill-list {
            font-size: 10pt;
            line-height: 1.6;
        }

        /* Footer Section */
        .footer {
            margin-top: 30px;
            padding-top: 10px;
            border-top: 1px solid #ccc;
            text-align: center;
            font-size: 8pt;
            color: #666;
        }

        .footer a {
            color: #666;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        /* Print Optimization */
        @media print {
            body {
                padding: 0;
            }

            /* Make footer darker and visible when printed */
            .footer {
                color: #333;
                border-top: 1px solid #666;
            }

            .footer a {
                color: #333;
            }

            /* Show URLs after link text when printing */
            .contact-info a[href^="http"]:after {
                content: " (" attr(href) ")";
                font-size: 9pt;
                color: #333;
            }

            .footer a[href^="http"]:after {
                content: " (" attr(href) ")";
                font-size: 7pt;
                color: #333;
            }
        }

        /* Ensure no page breaks in critical sections */
        .skill-category {
            page-break-inside: avoid;
        }
    </style>
</head>
<body>
    <!-- Header Section -->
    <div class="header">
        <h1>{{ header.name }}</h1>
        <h2>{{ header.title }}</h2>
        <div class="contact-info">
            {{ header.contact.email }}
            {%- if header.contact.phone %} | {{ header.contact.phone }}{% endif %}
            {%- if header.contact.location %} | {{ header.contact.location }}{% endif %}
            <br>
            {%- if header.contact.linkedin %}
            <a href="{{ header.contact.linkedin }}">LinkedIn</a>
            {% endif %}
            {%- if header.contact.github %}
            {%- if header.contact.linkedin %} | {% endif %}
            <a href="{{ header.contact.github }}">GitHub</a>
            {% endif %}
            {%- if header.contact.website %}
            | <a href="{{ header.contact.website }}">Website</a>
            {% endif %}
        </div>
    </div>

    <!-- Professional Summary -->
    <div class="section summary-section">
        <h3 class="section-title">Professional Summary</h3>
        <div class="summary-content">
            {{ summary.content }}
        </div>
    </div>

    <!-- Professional Experience -->
    <div class="section experience-section">
        <h3 class="section-title">Professional Experience</h3>
        {% for exp in experience.experiences %}
        <div class="experience-item">
            <div class="experience-header">
                <div class="job-title">{{ exp.title }}</div>
                <div class="company-name">{{ exp.company }}</div>
                <div class="job-meta">
                    {% if exp.location %}{{ exp.location }} | {% endif %}
                    {{ exp.start_date.strftime('%B %Y') }} -
                    {% if exp.is_current %}Present{% else %}{{ exp.end_date.strftime('%B %Y') }}{% endif %}
                </div>
            </div>
            {% if exp.achievements %}
            <ul class="achievements">
                {% for achievement in exp.achievements %}
                <li>{{ achievement }}</li>
                {% endfor %}
            </ul>
            {% endif %}
            {% if exp.technologies %}
            <div class="technologies">
                <strong>Technologies:</strong> {{ exp.technologies | join(', ') }}
            </div>
            {% endif %}
        </div>
        {% endfor %}
    </div>

    <!-- Education -->
    {% if education and education.education %}
    <div class="section">
        <h3 class="section-title">Education</h3>
        {% for edu in education.education %}
        <div class="education-item">
            <div>
                <strong>{{ edu.institution }}</strong> - {{ edu.degree }}{% if edu.field_of_study %}, {{ edu.field_of_study }}{% endif %}
            </div>
            <div class="education-meta">
                {% if edu.start_date and edu.end_date %}
                    {{ edu.start_date.strftime('%B %Y') }} - {{ edu.end_date.strftime('%B %Y') }}
                {% elif edu.end_date %}
                    Graduated: {{ edu.end_date.strftime('%B %Y') }}
                {% elif edu.graduation_date %}
                    Graduated: {{ edu.graduation_date.strftime('%B %Y') }}
                {% endif %}
                {% if edu.gpa %} | GPA: {{ edu.gpa }}{% endif %}
            </div>
        </div>
        {% endfor %}
    </div>
    {% endif %}

    <!-- Technical Skills -->
    <div class="section">
        <h3 class="section-title">Technical Skills</h3>
        <div class="skills-grid">
            {% set categories = namespace(list=[]) %}
            {% for skill in skills.skills %}
                {% if skill.category not in categories.list %}
                    {% set _ = categories.list.append(skill.category) %}
                {% endif %}
            {% endfor %}

            {% for category in categories.list %}
            <div class="skill-category">
                <div class="skill-category-title">{{ category }}</div>
                <div class="skill-list">
                    {% set category_skills = skills.get_by_category(category) %}
                    {{ category_skills | map(attribute='name') | join(', ') }}
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <!-- Footer -->
    {% if footer %}
    <div class="footer">
        {{ footer.text }}
        {% if footer.link %}
        | <a href="{{ footer.link }}">{{ footer.link_text or "Learn More" }}</a>
        {% endif %}
    </div>
    {% endif %}
</body>
</html>
